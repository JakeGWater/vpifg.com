{
    "scripts":{
        "start":"sphinx-autobuild -aE -b dirhtml --port 3000 --watch 'forks' source build",
        "install": "pip3 install -r requirements.txt",
        "incremental": "sphinx-build -b dirhtml --port 3000 source build"
    }
}