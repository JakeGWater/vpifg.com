{
    "scripts":{
        "start":"sphinx-autobuild -aE --port 3000 source build",
        "install": "pip3 install -r requirements.txt",
        "clean": "sphinx-build -aE -b html -vvvvv -T source build"
    }
}